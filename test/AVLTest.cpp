#include <avl/Tree.h>
#include <gtest/gtest.h>

#include <random>

namespace avl {

using TreeInt = Tree<int>;

TEST(AVLTreeTest, SimpleTest) {
    // TreeInt tree;
    // tree.insert(3);
    // EXPECT_EQ(tree.remove(3), 3);
}

}
